'use strict';

// Asagidaki `sifre` dizisinin elemanlarindan kaci `sayi (number)`, kaci `string` cinsindendir, bulunuz.

{
  const sifre = ['b', '$', 2, 1, 'X', 0, 0, 'a', 'c'];

  // Kodunuzu bu satirdan itibaren yaziniz
}
